# Session 001 — 2026-04-06 (morning)

## Summary

First session of the memory lab. Infrastructure milestone: memory profiling
activated and wired at key allocation sites.

## Rounds

| Round | Hypothesis | Result | Notes |
|-------|-----------|--------|-------|
| 01 | Enable AESTRA_ENABLE_MEMORY_PROFILING and wire macros at AudioBufferManager, Filter::resize, SampleRateConverter filter bank | ✅ accept | All 11 MemoryProfilingTest tests pass. All hard gates pass (MathTests, SampleRateConverter 22/22, Offline). Profiler now reports accurate allocation counts. |

## Outcome

- **1 round accepted**, **0 rounds rejected**
- Memory profiling is now active — `AESTRA_MEMORY_ALLOC/FREE` macros report to profiler
- 3 key allocation sites wired: AudioBufferManager, Filter::resize, SRC filter bank
- Baseline allocation tracking is operational

## Key Observations

1. **Profiler infrastructure was complete but unused** — The `AESTRA_MEMORY_ALLOC/FREE`
   macros were defined but never called at any real allocation site. Simply enabling
   the compile flag and wiring 3 sites makes the profiler functional.
2. **No code changes needed to profiler itself** — The existing `recordMemoryAllocation()`
   and `recordMemoryDeallocation()` implementations work correctly once called.
3. **All existing tests pass** — The profiling macros add zero overhead when the
   profiler is disabled, and when enabled they only increment counters (no allocations).

## Files Modified

- `AestraCore/CMakeLists.txt` — added `AESTRA_ENABLE_MEMORY_PROFILING`
- `AestraCore/src/MemoryProfilingTest.cpp` — new, 11 tests
- `AestraAudio/src/DSP/AudioProcessor.cpp` — wired alloc/free macros
- `AestraAudio/src/DSP/Filter.cpp` — wired alloc macro at resize
- `AestraAudio/src/DSP/SampleRateConverter.cpp` — wired alloc macro at filter bank creation
