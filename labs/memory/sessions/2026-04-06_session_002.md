# Session 002 — 2026-04-06 (late morning)

## Summary

Second session of the memory lab. Implemented arena allocator with lock-free
bump allocation. Integrated into AudioBufferManager.

## Rounds

| Round | Hypothesis | Result | Notes |
|-------|-----------|--------|-------|
| 01 | Implement AudioArena with atomic CAS bump allocation, 64-byte aligned buffer start, peak tracking | ✅ accept | 34/34 tests pass. Thread safety verified with 4 threads × 1000 allocs. |
| 02 | Wire AudioBufferManager to use GlobalAudioArena instead of new[] | ✅ accept | All hard gates pass. No allocations on audio thread after configure. |

## Outcome

- **3 rounds accepted** (02 + benchmark), **0 rounds rejected**
- `AudioArena`: lock-free bump allocator with atomic CAS loop
- `GlobalAudioArena`: 4 MB singleton
- `AudioBufferManager` → arena (no more `new[]`)
- `MemoryBenchmark`: 6 cases with real baselines and regression detection
- 34 allocator tests + 11 profiler tests + 22 resampler tests + Offline all pass

**Baseline numbers:**
| Case | Arena Allocs | Arena Bytes | Profiler Allocs | Time |
|------|-------------|-------------|----------------|------|
| bufmgr_construct | 1 | 262KB | 1 | 0.43ms |
| bufmgr_alloc_clear | 1 | 262KB | 1 | 0.82ms |
| src_configure_sinc16 | 0 | 0 | 1 | 1.54ms |
| src_process_sinc16 | 0 | 0 | 1 | 6.02ms |
| src_process_sinc64 | 0 | 0 | 1 | 11.74ms |
| src_roundtrip | 0 | 0 | 1 | 9.19ms |

## Key Observations

1. **Arena works for AudioBufferManager** — 1 arena alloc, 262KB used.
2. **SRC still uses raw new[]** — 0 arena allocs, 1 profiler alloc (the
   `AESTRA_MEMORY_ALLOC(sizeof(PolyphaseFilterBank))` call). This is a
   migration opportunity.
3. **Profiler bytes = 65568 for SRC** — the filter bank allocation size
   (256 phases × 64 taps × 4 bytes = 65536, rounded up).
4. **Eval now has real numbers** — no longer "if it compiles, it ships."
   Baseline comparison flags time >10% or allocation increases.

## Files Modified

- `AestraCore/include/AestraMemory.h` — AudioArena + GlobalAudioArena
- `AestraCore/src/MemoryAllocatorTest.cpp` — 34 allocator tests
- `AestraCore/CMakeLists.txt` — added test targets
- `AestraAudio/src/DSP/AudioProcessor.cpp` — AudioBufferManager uses arena
- `Tests/Integration/MemoryBenchmark.cpp` — new, 6 benchmark cases
- `Tests/CMakeLists.txt` — MemoryBenchmark target
- `labs/memory/run_eval.sh` — baseline comparison with JSON extraction
- `labs/memory/results/baseline.json` — captured baseline
