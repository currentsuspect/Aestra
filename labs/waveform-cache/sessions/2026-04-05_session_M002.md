# Waveform Cache Lab Validation Session M002

## Purpose

Validate that the new waveform-cache lab runs end to end against the real repo.

## Run

- Command: `./labs/waveform-cache/run_eval.sh --rebuild`
- Result: `accept`

## Hard-Gate Outcome

- `AestraWaveformCacheTest`: pass

## Advisory Outcome

- `AestraWaveformLockTest`: pass

## Notes

- The lab now has one deterministic correctness lane and one working advisory
  concurrency lane.
- The green summary was captured on a dirty creation tree, so a later clean-tree
  baseline capture is still recommended.
