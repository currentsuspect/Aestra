# Command History Lab Maintenance Session M001

## Purpose

Repair the lab so future agents can run bounded command-history sessions
against real repo targets.

## Work Performed

- Verified the actual CMake targets in `Tests/CMakeLists.txt`.
- Corrected lab documentation from nonexistent `Aestra*` target names to the
  real binaries:
  `CommandHistoryTest`, `MacroCommandTest`, `MoveClipCommandTest`,
  `ClipCommandsTest`, `MixerCommandsTest`, `CommandTransactionTest`.
- Repaired `run_eval.sh` to build and discover the real binaries.
- Added explicit session-budget and anti-gaming rules.
- Marked the lab honestly as correctness-only until a real benchmark exists.

## Rounds

- None. This was a maintenance-only pass.

## Outcome

- Lab status: usable for correctness sessions
- Remaining gap: first trusted green baseline still needs capture
- Remaining gap: no benchmark lane yet
