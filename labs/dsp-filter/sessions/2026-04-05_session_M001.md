# DSP Filter Lab Maintenance Session M001

## Purpose

Repair the filter lab so unattended correctness runs are real and bounded.

## Work Performed

- Verified the actual target surface in `Tests/CMakeLists.txt`.
- Confirmed that `AestraFilterTest` prompts for an optional interactive audio
  session after the automated checks.
- Repaired `run_eval.sh` to answer `n` and keep the correctness lane
  non-interactive.
- Tightened the lab docs to reflect reality: there is no trusted filter-only
  performance lane yet, and `AestraAudioPerformanceTest` is context only.
- Added session-budget and anti-gaming rules.

## Rounds

- None. This was a maintenance-only pass.

## Outcome

- Lab status: usable for autonomous correctness sessions
- Remaining gap: dedicated machine-readable filter benchmark still needed
