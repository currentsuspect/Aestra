# DSP Filter Lab Validation Session M003

## Purpose

Restore a green correctness surface for the DSP filter lab after the first
honest unattended run exposed real failures.

## Changes

- Fixed filter coefficient initialization so the live coefficients are exact
  after force updates and continue smoothing until convergence during runtime
  parameter changes.
- Fixed `FilterTest` response measurement to report normalized gain instead of
  raw sine RMS.

## Run

- Command: `./labs/dsp-filter/run_eval.sh`
- Result: `accept`

## Hard-Gate Outcome

- `AestraFilterTest`: pass

## Notes

- The unattended runner remains correct: it answers `n` to the optional audio
  prompt and still captures the full automated test output.
- The passing summary was captured on a dirty repair tree, so a future clean
  baseline capture is still recommended.
