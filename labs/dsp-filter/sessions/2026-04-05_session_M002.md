# DSP Filter Lab Validation Session M002

## Purpose

Validate that the repaired DSP filter lab runs unattended and reports real
hard-gate outcomes.

## Run

- Command: `./labs/dsp-filter/run_eval.sh`
- Result: `reject`

## Hard-Gate Outcome

- `AestraFilterTest`: fail

## Failure Notes

- The runner correctly answered `n` to the optional interactive prompt and
  completed unattended.
- Current HEAD failed the low-pass and high-pass checks in
  `AestraFilterTest`.
- This is a product/test-surface failure, not a lab-runner hang.

## Implication

- The lab is now honest and usable, but the subsystem is not currently at a
  green baseline. Filter optimization sessions should not proceed until the
  failing correctness surface is addressed.
